query worlds { 
  worldRecords @client{
    id
    deadCount,
    curedCount,
    confirmedCount,
    suspectedCount,
    recordAt

    confirmedCountAdd
    curedCountAdd
    deadCountAdd
    suspectedCountAdd
    confirmedCountAddRate
    curedCountAddRate
    deadCountAddRate
    suspectedCountAddRate
  }
}