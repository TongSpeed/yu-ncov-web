query worlds { 
  worldRecords {
    id
    deadCount,
    curedCount,
    confirmedCount,
    suspectedCount,
    recordAt

    confirmedCountAdd
    curedCountAdd
    deadCountAdd
    suspectedCountAdd
    confirmedCountAddRate
    curedCountAddRate
    deadCountAddRate
    suspectedCountAddRate
  }
}